# Screenshots

Drop project screenshots in this folder and keep these filenames so the root README can reference them cleanly:

- `current-ui.png` for the main lending dashboard 
- `current-ui-watch.png` for the liquidation watch tab
- `testing-suite.png` for Foundry test output or coverage screenshots

Recommended screenshot sources:

- current frontend UI running locally on `http://localhost:3002`
- Foundry test run from:

```bash
cd /home/bhavya_jain/Assignment/DefiApp/Contracts
~/.foundry/bin/forge test
```
